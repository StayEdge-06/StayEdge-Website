# Public env vars for production builds (NEXT_PUBLIC_* are client-safe by design)
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-Z8847F6ES9
NEXT_PUBLIC_CLARITY_PROJECT_ID=xozpmtluml

# Backend secrets (N8N, OS keys) must be set via Vercel dashboard — not committed.
